#include <iostream>
#include <vector>
#include "hashFunction.h"
#include "../item/item.h"
#include "../utils/utils.h"

using namespace std;

int main(void){

    /*
    h* r;
    string str = "hi";
    int x = 5,i;
    errorCode status;
    Item* p;
    vector<float> ve(5); 

   
    r = new hCosin(x);

    for(i = 0; i < 5;i++)
            ve[i] = i + 1;

    p = new Item(ve,status);

    r->print();

    cout << r->hash(*p,status) << "\n";
    p->print();


    delete r;
    delete p;
   */


    return 0;
}
